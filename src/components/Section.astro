---
const { tittle, fullWidth = false } = Astro.props;
---

<section class:list={[{ "section-full": fullWidth }]}>
  {tittle && <h2>{tittle}</h2>}
  <slot />
</section>

<style>
  section {
    max-width: 700px;
    margin: 0 auto 45px;
  }

  .section-full {
    max-width: none;
  }

  h2 {
    margin-bottom: 8px;
    font-weight: 700;
    line-height: 1.5;
    font-size: 1.2rem;
  }
</style>
